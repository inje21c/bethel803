# 성경 북마크, 읽기표, 1년 1독 설계

## 1. 설계 방향 재정의

2026-06-08 기준으로 성경 기능은 `본문 읽기`, `북마크`, `읽기 기록`에 더해 `읽기표 기반 성경 일독`을 별도 기능 축으로 확장한다.

첨부된 바이블 애플 이미지의 핵심은 단순한 "1년 1독 탭"이 아니라 사용자가 직접 읽기표를 만들고, 매일 분량을 체크하며, 전체 성경의 장 단위 진행률을 시각적으로 확인하는 경험이다. 따라서 기존 22번 문서의 "구약/신약 병행형 365일 챌린지" 초안은 다음 방향으로 조정한다.

1. `1년 1독 챌린지`보다 넓은 개념인 `읽기표`를 상위 모델로 둔다.
2. 기본 제공 읽기표는 `성경 전체, 성경순, 하루 3장, 시작일 기준`으로 만든다.
3. 사용자는 추후 `구약`, `신약`, `권별 선택`, `하루 장 수`, `시작일`, `주말 제외`를 선택할 수 있다.
4. 진행 단위는 절이 아니라 `장`이다. 성경 전체 1,189장을 체크 가능한 단위로 관리한다.
5. 본문 읽기 화면과 읽기표는 연결하되, 기존 수동 `bible_reading_logs` 입력은 유지한다.
6. 읽기표를 통해 완료한 장 수도 `bible_reading_logs`에 자동 저장해 수기 기록과 합산한다.
7. 읽기 기록 목록에서는 수기 기록만 수정 버튼을 제공하고, 읽기표 자동 기록은 완료 체크 표시만 보여준다.

## 2. 이미지 분석 요약

### 2.1 홈/진입 화면

이미지는 `도전!일독` 영역에서 두 가지 경로를 제공한다.

- 자유롭게 읽기
  - 현재 진행률 표시
  - `읽기표 만들기`
  - `읽기표 보기`
- 계획 세워 읽기
  - 날짜를 정해 계획 생성
  - `계획 만들기`

우리 앱에서는 이미 `성경` 메뉴가 있으므로, 별도 하단 메뉴를 만들기보다 성경 페이지 안에 `일독` 또는 `읽기표` 탭을 추가한다. 다만 화면 첫 진입에는 사용자가 바로 이해할 수 있도록 `내 읽기표` 카드와 `읽기표 만들기` 버튼을 제공한다.

### 2.2 읽기표 목록

목록 카드에는 다음 요소가 있다.

- 읽기표 이름
- 번역본
- 범위
- 진행 일수
- 진행률
- 대표 읽기표 설정
- 읽기표 체크하기
- 더보기 메뉴

우리 앱의 MVP에서는 사용자별 읽기표를 여러 개 만들 수 있게 DB는 열어두되, UI는 `대표 읽기표 1개` 중심으로 시작한다. 여러 읽기표 목록/대표 설정은 2차 범위로 둔다.

### 2.3 읽기표 만들기 플로우

이미지는 단계형 wizard로 구성된다.

1. 이름 입력
2. 참여 유형 선택: 혼자, 가족, 교회/그룹
3. 번역본 선택
4. 읽을 범위 선택: 전체, 구약, 신약, 권별 선택
5. 시작 날짜 선택
6. 주말 제외 여부
7. 하루 장 수 또는 완료 기간 선택
8. 생성 완료
9. 알림 시간 선택

우리 앱의 MVP는 다음만 포함한다.

- 이름
- 읽을 범위: 성경 전체, 구약, 신약
- 시작일
- 하루 장 수: 3, 5, 7, 10, 직접 입력
- 번역본 표시는 `개역개정` 고정

다음은 2차 범위로 미룬다.

- 가족/교회 그룹 공동 읽기
- 권별 선택
- 주말 제외
- 푸시 알림 시간
- AI 추천 계획
- 영상 성경

### 2.4 읽기표 대시보드

이미지의 핵심 대시보드 요소는 다음이다.

- 읽기표 제목과 기간
- 진행률
- 연속 읽기 일수
- 배지 수
- 전체 진행 바
- 이번 주 읽기 현황
- 오늘 분량
- `시작` 버튼
- 읽기계획/출석 탭

우리 앱에서는 MVP에서 다음을 제공한다.

- 대표 읽기표 요약 카드
- 진행률
- 완료 장 수 / 전체 장 수
- 오늘 날짜 기준 day 표시
- 오늘 읽을 범위
- `본문에서 읽기`
- `오늘 분량 완료`
- `읽은 장 체크`

연속 읽기, 배지, 출석 캘린더는 데이터 모델에는 확장 가능하게 열어두되, 첫 구현 화면에서는 제외한다.

### 2.5 일독 진행표

이미지는 성경 권별로 장 번호 원형 버튼을 나열한다. 각 장이 체크 가능한 단위이고, 권별 `본문보기` 버튼이 있다. 하단에는 현재 읽기표 진행률 오버레이가 있다.

우리 앱에서는 이 화면을 `읽기표 체크하기` 화면으로 설계한다.

- 왼쪽 또는 상단: 권 이름
- 오른쪽: 장 번호 그리드
- 완료 장: primary 색 또는 체크 표시
- 오늘 분량: 강조 테두리
- 현재 읽는 장: 별도 강조
- 장 클릭 시 `본문 읽기`로 이동
- 장 길게 누르기 또는 체크 버튼으로 완료 토글

## 3. 사용자 흐름

### 3.1 읽기표가 없는 사용자

1. `성경 > 읽기표` 탭 진입
2. 빈 상태 카드 표시
3. `읽기표 만들기` 선택
4. 이름, 범위, 시작일, 하루 장 수 선택
5. 생성 완료 화면 표시
6. 대표 읽기표 대시보드로 이동

### 3.2 읽기표가 있는 사용자

1. `성경 > 읽기표` 탭 진입
2. 대표 읽기표 요약 카드 표시
3. 오늘 분량 확인
4. `본문에서 읽기` 선택 시 첫 미완료 장 본문으로 이동
5. 본문 읽기 후 `오늘 분량 완료` 또는 장별 체크
6. 진행률과 완료 장 수 갱신

### 3.3 본문 읽기와 연결

읽기표에서 장을 열면 기존 `본문 읽기` 탭으로 이동한다.

- 선택 권/장으로 이동한다.
- 마지막 읽은 위치 저장 기능은 그대로 동작한다.
- 읽기표에서 온 경우 본문 상단 또는 하단에 `이 장 완료` 버튼을 노출한다.
- 이미 완료한 장은 완료 상태를 표시한다.

## 4. 화면 설계

### 4.1 성경 페이지 탭

기존 목표 탭 구조를 다음으로 확정한다.

```text
본문 읽기
북마크
읽기표
읽기 기록
```

`읽기 기록`은 기존 수동 장수 기록과 읽기표 자동 기록을 함께 보여주는 합산 기록이다. `읽기표`는 계획과 체크 기반 진행이다. 두 기능은 연결하되 같은 화면으로 합치지 않는다.

읽기 기록 목록 표시 기준:

```text
2026-06-08  4장                         [연필]
2026-06-08  3장  말씀을 읽는 기쁨 1일차  [체크]
```

합계와 주간 보고에는 두 기록을 모두 더한다. 사용자는 기존처럼 직접 장수를 입력할 수 있고, 읽기표에서 체크한 분량은 자동으로 별도 기록으로 들어온다. 화면에는 `수기`, `읽기표` 같은 출처 라벨을 크게 노출하지 않고, 오른쪽 액션만 다르게 보여 자연스럽게 구분한다.

### 4.2 읽기표 탭 빈 상태

```text
말씀으로 하루를 채우세요.

[자유롭게 읽기]
0% 진행 중
[읽기표 만들기]

[계획 세워 읽기]
날짜를 정해 계획을 만들어요
[계획 만들기]
```

MVP에서는 두 버튼 모두 같은 생성 wizard로 연결하되, 기본값만 다르게 둔다.

- 자유롭게 읽기: 시작일 오늘, 하루 장 수 직접 체크 중심
- 계획 세워 읽기: 시작일 오늘, 하루 3장 기본

### 4.3 읽기표 생성 wizard

MVP는 한 화면 또는 2단계로 시작한다. 모바일 화면에서는 단계형이 자연스럽지만, 개발 복잡도를 낮추기 위해 첫 구현은 한 화면 폼으로 만든다.

입력값:

- 이름: 기본값 `말씀을 읽는 기쁨`
- 번역본: `개역개정` 고정 표시
- 읽을 범위: `성경 전체`, `구약`, `신약`
- 시작일: 기본값 오늘
- 하루 장 수: `3`, `5`, `7`, `10`, `직접 입력`

계산값:

- 전체 장 수
- 예상 완료일
- 총 일수

생성 후 완료 카드:

```text
읽기 계획표가 완성되었습니다.

목표  성경 전체(개역개정)
방법  성경순 읽기
분량  매일 3장, 397일
기간  2026-06-08 ~ 2027-07-09

[완료]
```

### 4.4 대표 읽기표 대시보드

```text
완독 챌린지: 올해는 반드시 끝까지!
2026-06-08 ~ 2027-07-09

진행률 0%
연속읽기 0일
완료 0 / 1,189장

[진행 바]

이번주 읽기 현황
일 월 화 수 목 금 토

오늘 읽기
1일차 TODAY
창세기 1-3장

[본문에서 읽기]
[오늘 분량 완료]
[읽기 현황]
```

### 4.5 읽기 현황 화면

권별 장 그리드:

```text
창세기
1 2 3 4 5 ...

출애굽기
1 2 3 4 5 ...
```

상태:

- 미완료: 연한 회색 원
- 완료: 파란색 원 + 체크
- 오늘 분량: 파란 테두리
- 현재 선택: 진한 배경

액션:

- 장 번호 클릭: 본문으로 이동
- 장 체크: 완료 토글
- 권 이름 옆 `본문보기`: 해당 권 1장으로 이동

## 5. 데이터 모델

기준 데이터:

- `bible_books`
- `bible_verses`

읽기표 데이터:

```text
bible_reading_plans
- id uuid primary key
- church_id uuid null
- owner_user_id uuid not null
- title text not null
- translation text not null default '개역개정'
- scope text not null check in ('all', 'old', 'new', 'custom')
- mode text not null check in ('sequential')
- start_date date not null
- end_date date not null
- daily_chapter_target integer null
- total_chapters integer not null
- is_primary boolean not null default false
- status text not null check in ('active', 'completed', 'archived')
- created_at timestamptz
- updated_at timestamptz
```

하루별 계획:

```text
bible_reading_plan_days
- id uuid primary key
- plan_id uuid not null references bible_reading_plans(id)
- day_number integer not null
- scheduled_date date not null
- chapter_count integer not null
- completed_at timestamptz null
- created_at timestamptz
- unique(plan_id, day_number)
- unique(plan_id, scheduled_date)
```

하루 안의 장 항목:

```text
bible_reading_plan_day_items
- id uuid primary key
- plan_day_id uuid not null references bible_reading_plan_days(id)
- plan_id uuid not null references bible_reading_plans(id)
- sequence integer not null
- book_id smallint not null
- chapter integer not null
- completed_at timestamptz null
- reading_log_id uuid null
- created_at timestamptz
- unique(plan_id, book_id, chapter)
```

장 단위 완료를 `plan_day_items`에 직접 저장한다. 별도 progress 테이블만 두는 방식보다 권별 장 그리드와 오늘 분량 화면을 만들기 쉽다.

기존 읽기 기록 확장:

```text
bible_reading_logs
- id uuid primary key
- user_id uuid not null
- log_date date not null
- chapters integer not null
- source_type text not null default 'manual'
  check in ('manual', 'plan')
- source_label text null
- plan_id uuid null references bible_reading_plans(id)
- plan_day_id uuid null references bible_reading_plan_days(id)
- created_at timestamptz
- updated_at timestamptz
```

기존 테이블은 유지하고 컬럼을 추가한다. 이미 저장된 과거 기록은 모두 `source_type = manual`로 본다.

권장 제약:

```text
-- 수기 입력은 같은 날짜에 여러 번 입력할 수 있다.
-- 읽기표 자동 기록은 같은 계획의 같은 day에 하나만 생성한다.
unique(user_id, plan_day_id) where source_type = 'plan'
```

`source_label`은 화면 표시용 보조 값이다.

예:

- 수기 입력: `source_type = manual`, `source_label = null`
- 읽기표 1일차 자동 기록: `source_type = plan`, `source_label = '말씀을 읽는 기쁨 1일차'`

2차 확장 데이터:

```text
bible_reading_plan_members
- id
- plan_id
- user_id
- role
- joined_at

bible_reading_plan_reminders
- id
- plan_id
- user_id
- enabled
- remind_time
- created_at
```

## 6. 계획 생성 로직

### 6.1 범위별 장 목록

- `all`: bible_books 전체, book_order 순서
- `old`: testament = old
- `new`: testament = new
- `custom`: 2차 구현

각 권의 `chapter_count`를 기준으로 장 목록을 펼친다.

예:

```text
창세기 1
창세기 2
...
말라기 4
마태복음 1
...
요한계시록 22
```

### 6.2 일자 분배

MVP는 성경순 고정이다.

1. 선택 범위의 전체 장 목록을 만든다.
2. `daily_chapter_target` 개수만큼 순서대로 끊는다.
3. 각 묶음에 `day_number`와 `scheduled_date`를 배정한다.
4. 마지막 날은 남은 장 수만 배정한다.
5. `end_date = 마지막 scheduled_date`로 저장한다.

주말 제외가 켜진 경우는 2차 구현에서 scheduled_date 배정 시 토/일을 건너뛴다.

### 6.3 기본 옵션

성경 전체 1,189장 기준:

- 하루 3장: 397일
- 하루 5장: 238일
- 하루 7장: 170일
- 하루 10장: 119일

## 7. 기록 연동 원칙

기존 `bible_reading_logs`는 유지한다. 이 테이블은 주간 보고/수동 입력에서 이미 쓰이는 "읽은 장 수 합계"이다. 읽기표 자동 완료도 이 테이블에 별도 출처로 저장해 기존 통계와 자연스럽게 합산한다.

읽기표 완료와 수동 기록의 관계:

1. 읽기표의 장 완료는 `bible_reading_plan_day_items.completed_at`에 저장한다.
2. 오늘 분량 전체 완료 또는 개별 장 체크 시 해당 `plan_day`의 완료 장 수를 계산한다.
3. 계산된 장 수를 `bible_reading_logs`에 `source_type = plan`으로 upsert한다.
4. 같은 날짜에 사용자가 수동 입력한 `source_type = manual` 기록은 그대로 둔다.
5. 합계, 연간 진행률, 주간 보고는 `manual + plan` 전체를 더한다.
6. 사용자가 수동 읽기 기록을 수정해도 읽기표 체크를 자동으로 되돌리지는 않는다.
7. 사용자가 읽기표 체크를 해제하면 연결된 `plan` 기록의 장 수를 다시 계산하고, 0장이 되면 해당 자동 기록을 삭제한다.

중복 방지:

- 같은 plan 안에서 같은 `book_id + chapter`는 한 번만 완료 가능하다.
- 같은 plan의 같은 day는 `bible_reading_logs`에 자동 기록을 하나만 가진다.
- `reading_log_id`, `plan_id`, `plan_day_id`, `source_type`을 통해 읽기표에서 만든 기록인지 추적한다.

읽기 기록 화면 표시:

- `source_type = manual`: 기존처럼 오른쪽에 연필 수정 버튼 표시
- `source_type = plan`: 오른쪽에 완료 체크 표시만 표시
- `plan` 기록에는 필요 시 읽기표 이름과 day 번호를 작은 보조 텍스트로 표시
- `수기`, `읽기표` 같은 출처 배지는 기본 화면에 노출하지 않는다.
- 수정/삭제 권한은 출처별로 다르게 둔다.

수정/삭제 기준:

- 수기 기록: 기존처럼 장 수 수정/삭제 가능
- 읽기표 자동 기록: 읽기 기록 화면에서 직접 장 수 수정하지 않는다.
- 읽기표 자동 기록 삭제는 읽기표 체크 해제로 유도한다.
- 단, 운영상 꼬인 기록을 정리할 수 있도록 관리자 또는 추후 고급 메뉴에서 별도 정정 기능을 검토한다.

## 8. 권한과 범위

MVP는 개인 읽기표만 지원한다.

RLS:

- 본인 plan만 select/insert/update/delete 가능
- 기준 성경 데이터는 authenticated select 가능
- church_id는 향후 교회/그룹 읽기표 확장을 위해 nullable로 둔다.

2차에서 교회/그룹 읽기표를 추가할 때:

- owner_user_id는 생성자
- bible_reading_plan_members에 참여자를 저장
- 같은 구역/교회 사용자가 참여 가능
- 개인 완료 상태는 별도 member progress로 분리할지 재검토한다.

## 9. 개발 순서

### 9.1 MVP

1. DB 마이그레이션 작성
   - `bible_reading_plans`
   - `bible_reading_plan_days`
   - `bible_reading_plan_day_items`
   - `bible_reading_logs.source_type/source_label/plan_id/plan_day_id` 확장
2. 계획 생성 API 작성
   - 범위, 시작일, 하루 장 수 입력
   - 성경순 장 목록 생성
   - day/day_item 일괄 insert
3. 성경 페이지 탭 추가
   - `본문 읽기`
   - `북마크`
   - `읽기표`
   - `읽기 기록`
4. 읽기표 빈 상태/생성 폼 구현
5. 대표 읽기표 대시보드 구현
6. 오늘 분량 `본문에서 읽기` 연결
7. 장별 완료 토글 구현
8. 완료 시 `bible_reading_logs`에 `source_type = plan`으로 자동 기록 동기화
9. 읽기 기록 화면에서 수기 기록은 연필 버튼, 읽기표 자동 기록은 체크 표시로 구분

### 9.2 2차

1. 읽기표 목록과 대표 읽기표 설정
2. 권별 선택
3. 주말 제외
4. 연속 읽기/배지
5. 출석 탭
6. 알림 시간 설정
7. 가족/교회/그룹 공동 읽기

## 10. 기존 기능과의 관계

### 10.1 북마크

북마크는 절 단위 저장이다. 읽기표와 직접 결합하지 않는다.

- 본문에서 북마크 표시 유지
- 읽기표에서 본문으로 이동한 뒤에도 북마크 가능
- 북마크 목록에서 본문으로 이동하는 흐름 유지

### 10.2 마지막 읽은 위치

최근 구현한 마지막 위치 저장은 읽기표와 함께 사용한다.

- 일반 본문 읽기: 마지막 위치 복원
- 읽기표에서 진입: 읽기표가 지정한 권/장으로 이동
- 이후 사용자가 나가면 그 위치가 마지막 위치로 저장됨

### 10.3 읽기 기록

수동 읽기 기록은 제거하지 않는다. 구역원들은 이미 수기 장수 입력에 익숙하므로, 읽기표는 기존 습관을 대체하는 기능이 아니라 자동 보조 기록으로 붙인다.

- 앱 밖에서 읽은 장수 입력 가능
- 읽기표 완료분은 `source_type = plan`으로 자동 반영
- 읽기 기록 목록에는 출처 배지를 드러내지 않는다.
- 수기 기록은 오른쪽에 연필 수정 버튼을 유지한다.
- 읽기표 자동 기록은 오른쪽에 체크 표시만 보여주고 직접 수정은 막는다.
- 연간 합계, 주간 합계, 주간 보고는 계속 `bible_reading_logs` 기준으로 계산
- 따라서 수기 입력분과 읽기표 자동 입력분은 자연스럽게 합산된다.

## 11. 롤백 기준

읽기표는 기존 본문/북마크/수동 읽기 기록과 분리된 탭으로 추가한다.

문제가 생기면:

1. `읽기표` 탭을 숨긴다.
2. 읽기표 관련 API 호출을 중단한다.
3. 기존 `본문 읽기`, `북마크`, `읽기 기록`은 계속 사용한다.

읽기표 테이블은 삭제하지 않고 보존한다. 이미 생성된 사용자의 계획 데이터는 수정 배포 후 다시 사용할 수 있게 한다.
