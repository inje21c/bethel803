# 개발 일지 — 2026-06-07 ~ 2026-06-15

> 작업 환경: 회사 (Claude Code)
> 대상 저장소: bethel803 (벧엘구역 소모임 커뮤니티 앱)

---

## 2026-06-15 (일)

### P0 SaaS — feat/qt-saas 브랜치 main 머지 완료

**핵심 기능 완성 및 안정화**

- **교회 설정 ChurchProvider 단일화** (`perf`)
  - `useChurch()` 훅으로 교회 설정을 한 번만 로드, 중복 쿼리 및 게이팅 깜빡임 제거
  - `ChurchProvider` 도입으로 컨텍스트 분리

- **인증 페이지 UX 개선** (`ux`)
  - 로그인 페이지 하단 세로 여백 확보 → "교회 등록" 링크 가시성 향상

- **회원가입 UX 정리** (`ux`)
  - 교회 등록 / 랜딩 페이지에서 "신용카드 불필요" 문구 제거 (혼란 방지)
  - 교회 등록 vs 초대 합류 경로 명확히 분리

- **버그 수정**
  - `getMyChurchSettings` Promise.allSettled로 부분 실패 허용
  - legacy 플랜 교회 bible_text 모듈 게이팅 오작동 수정
  - 웹 타이틀/메타 설명 "벧엘구역"으로 통일

- **테넌트(교회) 격리 전수 테스트** (`test`)
  - 교회 경계 테스트 스크립트 작성 (`scripts/`)
  - 테스트 절차 문서화

- **DB 마이그레이션 prod 전체 적용 완료** (001~033)
  - 021~026: SQL Editor 수동 실행
  - 027~033: GitHub Actions prod 워크플로우 자동 배포

---

### feat/qt-saas 브랜치 작업 (PR #1 → main 머지)

- **교회 셀프가입 플로우** (`feat`, `032 마이그레이션`)
  - `/signup/church` 페이지, 30일 무료 체험
  - `handle_new_user` 트리거: 교회 + 구역 + master 자동 생성

- **Trial/Free 기능 제한** (`feat`)
  - `hasModule()` 헬퍼로 기능 게이팅
  - bible_text 라이선스 게이팅 (trial 중 false)
  - 체험 만료 배너, 온보딩 페이지 (`/onboarding`)
  - 랜딩 페이지 (`/`), 이용약관/개인정보처리방침 (`/terms`, `/privacy`)

- **구성원 초대 링크** (`feat`)
  - `/join?d=<district_id>` — 이메일/구글/카카오, 구역 자동 배정
  - `assign_my_district` RPC

- **회원탈퇴** (`feat`)
  - Edge Function `delete-account` — master 단독 시 교회 전체 삭제
  - JWT 검증 방식 수정 (`adminClient.auth.getUser`)

- **성경공부 답변 비공개** (`privacy`)
  - 구역장 포함 관리자도 타인 답변 내용 열람 불가
  - 완료 여부만 확인 가능

- **사용자 매뉴얼 전면 개편** (`docs`)
  - 전문 용어 제거, 쉬운 말로 재작성

- **브랜드명 통일** (`ux`)
  - 전체 "벧엘구역"으로 일관 적용

---

## 2026-06-14 (토)

- **Simple QT 말씀 책 교회별 설정** (`feat`) — C안 채택
  - 교회 설정에서 QT 본문 출처 선택 가능

- **성능 개선** (`perf`)
  - SW precache 46% 감소
  - Supabase preconnect 힌트 추가
  - Vercel 빌드 에러 수정 (globIgnores 방식)

- **Simple QT 생성 실패 시 에러 원인 노출** (`fix`)
  - qtMode queryKey 포함

---

## 2026-06-12 (목)

- **성경 읽기 계획 버그 수정** (`fix`)
  - 333일 이후 계획 잘림 수정
  - 데스크톱에서 설정 UI 너비 확장
  - 폰트 슬라이더 reader 탭에만 적용
  - 빈 상태 중첩 박스 제거
  - 가로 화면 탭 레이아웃 정렬 일치

- **CI/CD 개선**
  - supabase CLI npm 설치로 GitHub API rate limit 우회
  - Node 24 액션 런타임 opt-in
  - Supabase 마이그레이션 + Edge Function 배포 워크플로우 추가
  - 성경 시드 수동 워크플로우 (staging/prod)
  - 성경 시드 SQL gitignore, JSON에서 생성

- **멀티테넌트 버그 수정**
  - 구역 목록이 타 교회에 노출되는 tenant leak 수정
  - SW 세션 중 강제 reload 방지

- **QT 기능**
  - 대시보드 QT 요약 fallback 추가
  - 제목/말씀 중복 표시 제거

- **교회 설정 및 QT Simple 모드** (`feat`, SaaS step 1)
  - `church_settings` 테이블
  - QT simple 모드 (말씀 카드만)

- **QT UX 개선** (`feat`)
  - 4단계 깊은 묵상 플로우 추가
  - 뒤로가기 내비게이션, 완료 후 수정, 월별 탐색, AI 캐싱

- **비밀번호 재설정 redirect fallback** (`fix`)
  - 현재 origin 기반 동적 처리

---

## 2026-06-11 (수)

- **비밀번호 찾기 플로우 재설계** (`feat`)
  - 복구 옵션 다이얼로그 도입

- **카카오 로그인 추가** (`feat`)
  - 기존 구글 로그인에 카카오 병행 지원

- **구글 로그인 추가** (`feat`)
  - 비밀번호 로그인 외 소셜 로그인 도입

- **master 임시 비밀번호 발급** (`feat`)
  - 구성원에게 임시 비밀번호 발급 기능

- **가입 시 구역 선택 필수화** (`feat`)

- **앱 아이콘 PNG 추가** (`chore`)
  - 외부 서비스 등록용

- **문서 작업** (`docs`)
  - 비밀번호 찾기 플로우 문서화
  - Branches 온보딩 분석 및 일반화 원칙
  - 경쟁 서비스 분석 및 확장 전략
  - Auth 설계/설정/확장 노트
  - 021 SaaS 마이그레이션 복원 + 확장 계획

---

## 2026-06-09 (월)

- **성경 읽기 계획 초기화 시 1챕터 자동 로그** (`fix`)
- **성경 리더에서 계획 챕터 완료 처리** (`feat`)

---

## 2026-06-08 (일)

- **성경 읽기 계획 전체** (`feat`)
  - 읽기 계획 추적 기능 추가
  - 계획 수정 허용
  - 챕터 이동 시 첫 절로 스크롤
  - 성경 리더 위치 기억 (localStorage)
  - devotional API 호출 캐시 fallback으로 감소

---

## 2026-06-07 (토)

- **성경 리더 구조화** (`feat`)
  - 성경 리더 탭 추가
  - 북마크 탐색 개선

- **UI 정리**
  - 성경 도전 DB 전략 문서화
  - 스터디 상태 인디케이터 크기 조정
  - 중보기도 대시보드 카드 정리
  - 북마크 목록 버그 수정

---

## 오늘 (2026-06-15 회사) — 별도 작업

- **fetch-devotional Edge Function 버그 수정**
  - 증상: `42P10` — `ON CONFLICT (date)` unique 제약 없음
  - 원인: 028 마이그레이션이 `date` 단독 UNIQUE → `(church_id, date)` 복합 인덱스로 교체했으나 Edge Function 미반영
  - 수정: `church_id` 명시 + `onConflict: 'church_id,date'` 변경
  - prod 배포 완료, 오늘(2026-06-16) 묵상 데이터 정상 저장 확인
